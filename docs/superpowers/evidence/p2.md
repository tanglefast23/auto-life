# P2 Evidence — headless game loop (started 2026-07-30)

## §0 Definition of Ready (recorded before Task 1)

| Check | Result |
|---|---|
| P1 exited | `npm run verify` exit 0 on f68891e; 78 tests / 9 suites; closure-day exact; mid-Meal JSON round-trip green |
| Real `src/sim/` exports reconciled | Plan-quoted signatures checked against the tree: P1 ships `startTimedActivity/progressTimedActivity/napEligibility/crossedWakeBoundary/restoreActiveTimedActivity/sleepContribution` (activities), `applyBarContributions/assertValidBars` (bar-deltas), `passiveContribution` (rates), `mSpeedAtStart/mOutAtStart/isWellFedAtStart/walkTilesPerMinute/healthDisplay` (bars), `PrngStreams` (prng), clock helpers, `content/activityById` registry. The tree wins wherever the draft guessed. |
| `ENGINE_VERSION` | exists, = 1 |
| Content schemas as built | `effectiveUsesPerDay`/`startBelow`/`effectiveWindow`/`suppressPassiveEnergyWhenEffective` (the draft's `restoresOnlyFirstOfDay` concept) survived P1 review in this richer form; two-decimal + magnitude bounds added in P1 rounds 2–3 |
| §1 blocking questions | **All six ruled 2026-07-29** (SPEC §0aa): Q1 P2 owns Practice scoring · Q2 per-run sorting · Q3 missed = unstarted at close · Q4 anchor fires once/day incl. deletion · Q5 single-rest-card · Q6 record travel-inclusive before asserting |
| Library guidance | zod 4 `partialRecord`/strictObject patterns already proven in P1; jest-expo 57 unchanged |
| §2 traceability | table present in the plan; completed at T17 below |

## Task log

- T1–T2: DoR + planner content, committed.
- T4–T5: home map (24×14, hall-doored rooms), 14 objects, walk grid, deterministic A* (total-order ties), integer travel ticks. **Plan erratum:** the draft quoted the travel denominator as `3×(150000+movementFixed)`; the correct fixed-point constant for 0.75 is 900000 — the tree wins, recorded here.

- T6–T10: queue (§7.4 one test per row), two-stage priority, anchor windows (Q3/Q4 as code), reactive exclusive groups (Q5), per-run sorting (Q2) + 10k-edit PINNED property test — all green first run.
- T3+T12: SimState (validated, JSON-safe) + the six-stage `step()`; Day-1 wake window observable on the first step; full unattended day; determinism (byte-identical at tick 2000); mid-run round-trip resume.
- T13–T14: Commitments DTO (type+validator only — materialization is v2's); forecaster clones state through the validator and runs the same step() path; tick-for-tick agreement test.
- T15–T16: golden week recorded (digest snapshot + ENGINE_VERSION 1 pinned); balance harness against `content/harness-bands.json`.

## Q6 record-before-assert — travel-inclusive figures (seed 1234)

- Morning check steady state (Day 3+): E 90 · N 77.4 · M 69.13 · H 95.4 — Movement −1.9 vs the pre-travel 71 (within tolerance).
- **Day-2 Movement transient = 63** → fairness floor ≥65 applies from Day 3; Day 2 holds a recorded ≥60 Movement floor. SPEC §6.8 re-derived (no ENGINE_VERSION change — the earlier trace was incomplete, not the behavior wrong).
- Nutrition min-over-all-ticks 47.53, pre-breakfast ✓ (band 50±3 holds at its low edge).
- Transit ≈26–27 game-min/day → §6.8 transit line re-derived to 20–50.
- Neglect (workout anchor disabled): Movement min 17.77 in 3 days; the Stretch net floors it ≈18 — `<20 within 3 days` band holds, and it is fail-soft, not free-fall.
- Recovery (all bars 0): end-of-Day-2 health 67.6 — the old ≥70 assertion sampled the designed pre-breakfast trough (the round-1 sampling-error class); re-derived to **Day-3 morning check ≥75** (measured ≈83).
- Zero urgent events, zero missed anchors, 7/7 days: toilet 1 · brush 2 · shower 1 · meal 3 · weights 1 — the routine is exactly §7.1's design.

## Replay format (T15, canonical for P4/P5 extensions)

`{ engineVersion, seed, chronotype, commands: [...ordered], digest: per-day { morningCheck, minPerBar, completions, travelMinutes } + firstWakeOrder + urgentCount + anchorsMissed }` — stored as a jest snapshot; a digest diff with unchanged ENGINE_VERSION fails as a bug.

## Adjacency coverage deferrals (recorded per T11)

Unattended run exercises **it-sticks (workout→meal)** only. Player-dependent pairs — warmed-up, cramp, minty-fresh, fresh-mind — are asserted by the **scripted-player golden (P4 owner)**; claiming them here would repeat the error round 2 split the goldens to fix. The engine paths exist and are unit-covered via step-level practice/adjacency tests.

## §2 traceability — completed

| SPEC clause | Test |
|---|---|
| §6.7 pairs + lifecycle | step.ts integration (warmed-up/cramp/it-sticks/minty/fresh-mind paths) + content gate; player-dependent coverage → P4 scripted golden |
| §6.8 travel-inclusive bands | balance.test.ts + harness-bands.json |
| §7.1 windows/blocks/missed/sleep/chronotype | queue-planner.test.ts (anchors) + step.test.ts |
| §7.2 triggers/exclusivity/suppression | queue-planner.test.ts (reactive) |
| §7.3 two-stage scores | queue-planner.test.ts exact-value tests |
| §7.4 every row | queue-planner.test.ts + queue.ts invariants |
| §7.5 forecast agreement/horizon/uncertainty | forecast.test.ts |
| §7.6 commitments DTO | forecast.test.ts round-trip |
| §7.7 tick order | step.test.ts (first-step wake window, single-writer, determinism) |
| §8 practice scoring/curves/block | step.test.ts + practice content gate |
| §16.2 determinism | step determinism + round-trip tests |
| §16.3 P2 subset | golden-week + balance harness |
| §10 grid/objects/A* | travel.test.ts + validate-home.test.ts |

## P2 exit (master §5)

- Full unattended placeholder week incl. travel: **golden-week.test.ts** green.
- Practice math green: step.test.ts award test.
- Replay diff tied to ENGINE_VERSION: golden asserts version 1 + snapshot.
- Forecast agrees with the real step path: forecast.test.ts.
- Queue invariants green: 10k-edit property test.
- `npm run verify` + `npm run harness`: both exit 0 (134 + 6 tests).
