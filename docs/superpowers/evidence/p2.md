# P2 Evidence — headless game loop (started 2026-07-30)

## §0 Definition of Ready (recorded before Task 1)

| Check | Result |
|---|---|
| P1 exited | `npm run verify` exit 0 on f68891e; 78 tests / 9 suites; closure-day exact; mid-Meal JSON round-trip green |
| Real `src/sim/` exports reconciled | Plan-quoted signatures checked against the tree: P1 ships `startTimedActivity/progressTimedActivity/napEligibility/crossedWakeBoundary/restoreActiveTimedActivity/sleepContribution` (activities), `applyBarContributions/assertValidBars` (bar-deltas), `passiveContribution` (rates), `mSpeedAtStart/mOutAtStart/isWellFedAtStart/walkTilesPerMinute/healthDisplay` (bars), `PrngStreams` (prng), clock helpers, `content/activityById` registry. The tree wins wherever the draft guessed. |
| `ENGINE_VERSION` | exists, = 1 |
| Content schemas as built | `effectiveUsesPerDay`/`startBelow`/`effectiveWindow`/`suppressPassiveEnergyWhenEffective` (the draft's `restoresOnlyFirstOfDay` concept) survived P1 review in this richer form; two-decimal + magnitude bounds added in P1 rounds 2–3 |
| §1 blocking questions | **All six ruled 2026-07-29** (SPEC §0aa): Q1 P2 owns Practice scoring · Q2 per-run sorting · Q3 missed = unstarted at close · Q4 anchor fires once/day incl. deletion · Q5 single-rest-card · Q6 record travel-inclusive before asserting |
| Library guidance | zod 4 `partialRecord`/strictObject patterns already proven in P1; jest-expo 57 unchanged |
| §2 traceability | table present in the plan; completed at T17 below |

## Task log

- T1–T2: DoR + planner content, committed.
- T4–T5: home map (24×14, hall-doored rooms), 14 objects, walk grid, deterministic A* (total-order ties), integer travel ticks. **Plan erratum:** the draft quoted the travel denominator as `3×(150000+movementFixed)`; the correct fixed-point constant for 0.75 is 900000 — the tree wins, recorded here.

- T6–T10: queue (§7.4 one test per row), two-stage priority, anchor windows (Q3/Q4 as code), reactive exclusive groups (Q5), per-run sorting (Q2) + 10k-edit PINNED property test — all green first run.
- T3+T12: SimState (validated, JSON-safe) + the six-stage `step()`; Day-1 wake window observable on the first step; full unattended day; determinism (byte-identical at tick 2000); mid-run round-trip resume.
- T13–T14: Commitments DTO (type+validator only — materialization is v2's); forecaster clones state through the validator and runs the same step() path; tick-for-tick agreement test.
- T15–T16: golden week recorded (digest snapshot + ENGINE_VERSION 1 pinned); balance harness against `content/harness-bands.json`.

## Q6 record-before-assert — travel-inclusive figures (seed 1234)

**ENGINE_VERSION 1 figures (superseded — kept for the audit trail):**

- Morning check steady state (Day 3+): E 90 · N 77.4 · M 69.13 · H 95.4.
- Day-2 Movement transient = 63.
- Nutrition min-over-all-ticks 47.53, pre-breakfast; transit ≈26–27 game-min/day; neglect Movement min 17.77 in 3 days; recovery end-of-Day-2 health 67.6.
- Zero urgent events, zero missed anchors, 7/7 days: toilet 1 · brush 2 · shower 1 · meal 3 · weights 1.

**ENGINE_VERSION 2 figures (current, after the critic-round-1 behavior fixes):**

- Morning check steady state (Day 3+): E 90 · N 77.4 · **M 81.1** · H 95.4 — Movement rose ~12 points because it-sticks now actually fires (see round-1 findings below).
- Day-2 Movement = **74.98** → the §6.8 morning-check floor is now a clean **≥65 from Day 2** with no Day-2 exemption.
- Nutrition min 47.5 pre-breakfast (band [47, 53] holds); §6.8 names the −4.6 steady-state deviation beyond the ±3 tolerance honestly rather than widening the band.
- Transit 24–27 game-min/day (band [20, 50]); neglect and urgent/missed counts unchanged.
- Recovery (all bars 0): **Day-3 morning-check health 77.27** (floor ≥75).
- `content/harness-bands.json` is the normative source for all bands (SPEC §16.3 defers to it); its `recordedAt` line carries these v2 measurements.

## Replay format (T15, canonical for P4/P5 extensions)

`{ engineVersion, seed, chronotype, commands: [...ordered], digest: per-day { morningCheck, minPerBar, completions, travelMinutes } + firstWakeOrder + urgentCount + anchorsMissed }` — stored as a jest snapshot; a digest diff with unchanged ENGINE_VERSION fails as a bug.

## Adjacency coverage deferrals (recorded per T11)

**Correction (critic round 1):** the original version of this section claimed the unattended run "exercises it-sticks (workout→meal)". That claim was **false at ENGINE_VERSION 1** — adjacency was measured at activity *completion* instead of *start*, so the workout-END→dinner-START gap always exceeded 60 min and it-sticks never fired in the golden week. The claim was written from the design intent, not from a trace; the critic caught it by instrumenting the run. Fixed in v2 (adjacency sampled in `beginCard`), and the v2 golden now genuinely exercises it-sticks daily. Player-dependent pairs — warmed-up, cramp, minty-fresh, fresh-mind — remain asserted by the **scripted-player golden (P4 owner)**; the engine paths are unit-covered via step-level practice/adjacency tests.

## §2 traceability — completed

| SPEC clause | Test |
|---|---|
| §6.7 pairs + lifecycle | step.ts integration (warmed-up/cramp/it-sticks/minty/fresh-mind paths) + content gate; player-dependent coverage → P4 scripted golden |
| §6.8 travel-inclusive bands | balance.test.ts + harness-bands.json |
| §7.1 windows/blocks/missed/sleep/chronotype | queue-planner.test.ts (anchors) + step.test.ts |
| §7.2 triggers/exclusivity/suppression | queue-planner.test.ts (reactive) |
| §7.3 two-stage scores | queue-planner.test.ts exact-value tests |
| §7.4 every row | queue-planner.test.ts + queue.ts invariants |
| §7.5 forecast agreement/horizon/uncertainty | forecast.test.ts |
| §7.6 commitments DTO | forecast.test.ts round-trip |
| §7.7 tick order | step.test.ts (first-step wake window, single-writer, determinism) |
| §8 practice scoring/curves/block | step.test.ts + practice content gate |
| §16.2 determinism | step determinism + round-trip tests |
| §16.3 P2 subset | golden-week + balance harness |
| §10 grid/objects/A* | travel.test.ts + validate-home.test.ts |

## Critic round 1 (3 critics: harsh indie · plan compliance · correctness/math) — all findings fixed

**ENGINE_VERSION 1 → 2.** Several fixes change simulated behavior, not just code shape; the golden digest legitimately differs, so the version was bumped and the golden re-recorded (the digest-diff-with-unchanged-version tripwire stays honest). The changelog lives as a comment in `src/sim/version.ts`.

| # | Severity | Finding | Fix |
|---|---|---|---|
| 1 | BLOCKER | Bedtime anchor was missable (window closed at wake+990) — a busy evening could cascade into a night with no sleep block | `anchors.json` bedtime `closesAt: 1439` (open-ended); a pinned-heavy evening delays sleep but never cancels it |
| 2 | BLOCKER | Running cards could be evicted/auto-cleaned mid-activity | running card exempt from reactive eviction and auto-cleanup (`runningCardId` filter in step stages) |
| 3 | BLOCKER | Adjacency measured at activity *completion*, so gap windows (≤15–60 min from previous END to next START) never matched; it-sticks never fired; evidence claimed otherwise | adjacency sampled at `beginCard` (start), data-driven from `adjacency.json`, re-trigger replaces (never self-stacks); evidence corrected above |
| 4 | major | Anchor blocks sorted below plain reactives | `BLOCK_TIER = 500` (urgent 1000+ > blocks 500 > stage-2 ≤4) |
| 5 | major | New AUTO/urgent cards appended at queue end instead of earliest run | `insertIntoEarliestRun` (after the leading PINNED prefix, §7.4) |
| 6 | major | Practice multiplier read at completion, not frozen at start | `dto.sampled.pointsMultiplier` captured in `beginCard` |
| 7 | major | Missed-window sweep removed PINNED cards | AUTO-only filter in the miss sweep |
| 8 | major | Stop suppressed the activity type even when the stopped card was the player's own PINNED card | suppression keys on `card.owner === 'AUTO'` |
| 9 | major | Player-inserted nap outside its window would start anyway | `beginCard` drops cards failing `canStart` |
| 10 | major | Hygiene-focus practice modifier (below 40 → ×0.75) missing | `practice.json` `hygieneFocus` + multiplier chain |
| 11 | major | Shuffled-expansion determinism test was a placebo (compared `findPath` to itself) | real `neighbourOrder` permutation test; A* ties break by total order |
| 12 | follow-up | Reactive evaluator treated only its own cards as exclusive-group occupants: it double-booked food around the running/queued **anchor breakfast** (snack card alongside the wake block's meal) | group occupancy now counts the running activity and any queued group member; non-reactive occupants (anchor/player) mark the need handled — Q5 eviction still works because a queued nap *is* reactive |
| 13 | follow-up | Busy-evening test asserted zero missed anchors of any kind; the day-2 WAKE anchor legitimately misses when leftover pins run first after waking (§7.4 pins are never crossed; Q3 close-unstarted = missed) | test narrowed to the actual guarantee: bedtime never misses; wake miss documented as designed behavior |

**Recorded deviations (process):** (a) round-1 fixes were committed in batches rather than task-per-commit — P3 restores strict task-per-commit; (b) the T3 commit was mislabeled (T3 content landed in the T12 commit); (c) T14 why-lines, cap-waste projection, and refresh triggers are deferred to P4 (UI owners) — the forecaster ships state-cloning + agreement only; (d) the cap-waste band is a coarse proxy: `balance.test.ts` counts cap-clamped fill ticks at ANY meal (lunch included) and divides by 7 against `mealCapWastePerDayMax` — renamed in round 2 from the misleading `dinnerCapWasteMax`; the full §6.6 waste audit is P4's.

## Critic round 2 (3 fresh critics, re-verify + new hunt) — all 13 round-1 fixes confirmed by probe; 5 new majors found and fixed

All three critics independently re-executed the round-1 fixes and confirmed them real (one ran a 50k-case adversarial fuzz of the reactive refactor: zero double-adds, zero wrong evictions; another reproduced every v2 Q6 figure from independent probes). New findings, all fixed — **ENGINE_VERSION 2 → 3** (the unattended digest is byte-identical to v2's except the version field; every v3 change bites only under player pressure, which is why round 1 missed them):

| # | Severity | Finding (critic) | Fix |
|---|---|---|---|
| 1 | major | §7.2 row 6 unimplemented: urgency sampled only at card creation — a queued meal stayed non-urgent while Nutrition crashed to 0.3 (sim showered at N 0.3 before eating); probes produced 14 h at Energy 0.0 with `urgentCount 0` (indie + math, same root) | urgency is LIVE: crossing-based per-bar crisis counting (`urgentActive` state, one urgent event per <15 crossing regardless of card churn) + promotion/demotion of queued cards serving the bar; a promoted card hits the stage-1 tier and outranks blocks — from all-zeros the first completion is now **meal**, not shower |
| 2 | major | Phantom bedtime: a block overtaken by urgent sleep executed at next wake (0-min sleep), and `beginCard` consumed the anchor for *today* instead of the block's day — the NEW day's bedtime vanished, producing a voluntary all-nighter (indie) | consumption keys the blockId's own day (beginCard AND player deletion); sleep-end retires stale prior-day blocks (consumed for their day, not counted missed). Re-probe of the 8-pins-at-21:00 trace: one sleep every night, zero phantom sleeps, zero bedtime misses |
| 3 | major | Nap chains: after the daily effective nap, the E<30 rule kept re-adding zero-restore naps — 3 h of visible "resting" while Energy fell to urgent (indie) | the planner only books EFFECTIVE naps (add-gate + beginCard + pre-travel gate use `.effective` for reactive cards); a player-pinned flavor nap still runs by choice. Re-probe: exactly one nap where the chain produced four |
| 4 | major | §6.7 "stopping either activity early cancels the bonus" unimplemented — a 2-min meal start-stop banked the 12 h it-sticks modifier; a stopped sleep kept `mintyArmed` (plan + math) | DTO records granted modifier sources; sleep records its own arming; `stopCurrent` revokes exactly what THIS instance granted — both directions tested |
| 5 | major | Evidence/traceability certified warmed-up/cramp/fresh-mind as "unit-covered" — zero test executions existed (plan; the engine paths themselves worked when probed) | three exact step-level tests added (halved duration; −60000 fixed cramp delta; ×1.2 sample ratio); the traceability claim is now true |
| 6 | minor | Forecast off-by-one: every predicted start/conflict one minute late (post-advance snapshot minute); `barsAtHorizon` overshot by a tick (math) | forecast reads the pre-advance minute and returns bars exactly at the horizon; agreement test tightened to exact-minute equality |
| 7 | minor | 11th `insertPlayer`/`objectClick` threw and aborted the whole tick (indie + math) | per-command rejection in stage 1 (`canPlayerInsert` guard); the pure queue functions still throw for P4's pre-validation |
| 8 | minor | Stage 3 traveled to guaranteed-droppable cards (walk-to-couch-then-shrug) and idled a tick after any drop (math + indie) | startability gates run before travel via shared `cardCanBegin`; a dropped card yields the slot to the next card the same tick; beginCard keeps the arrival-time backstop |
| 9 | minor | `removeCard` of the travel target left the sim walking to nothing (math) | removing the travel target clears `current` |
| 10 | minor | Double breakfast: a supersession-created meal card cleaned at the parent snack threshold (45) instead of its own (30), surviving to run back-to-back with the anchor breakfast (indie + math) | auto-cleanup keys the threshold that created the card (`supersededBelow.value + 10`) |
| 11 | minor | `urgentCount` inflated when the player fought an urgent card (delete ×5 → count 6) (indie) | subsumed by fix 1: crossing-based counting — probe: 6 delete/re-add cycles in one crisis count 1 |
| 12 | minor | Two tuning values hard-coded in `balance.test.ts` (plan) | `dinnersMinPerWeek` moved into `harness-bands.json`; the nutrition-min window and fail-soft floor stay in the test as structural assertions |
| 13 | minor | Continue-sleeping clause and §7.4 dedup untested; forecast wake-branch unexercised (plan) | direct tests added: night sleep continues past E80 to exact wakeTarget; dedup unit test; 23:00 forecast horizon = 480 |

**Recorded deferrals from round 2 (not fixed, by design):** `targetAt` in anchors.json is currently dead data — every anchor fires at window-open; P4's forecast/why-lines must either use it or P4 deletes it (decision owner: P4 plan). Stretch spacing at Movement ≈0 (4+ back-to-back stretches — productive, reads twitchy) is a §16 tuning question for P4.5's balance pass. The 1-tick completion→start gap and the overnight-snack-card-beside-the-wake-block are P4 presentation concerns. The wake/lunch misses under heavy pinning are §7.4's designed pins-first outcome (the recap and pin-warning UI own the player communication).

**Scripted-pressure figures (v3, seed 1234):** 8 practices pinned 21:00 → 1 sleep/night, 0 phantom sleeps, 0 bedtime misses, urgentCount 4 (real crises, honestly counted); 3 practices pinned 21:30 → exactly 1 nap next day; recovery from all-zeros → first completion meal, day-3 morning-check health 77.27 (floor 75), urgentCount 3 (one per starved bar).

## P2 exit (master §5)

- Full unattended placeholder week incl. travel: **golden-week.test.ts** green.
- Practice math green: step.test.ts award test.
- Replay diff tied to ENGINE_VERSION: golden asserts **version 3** + snapshot.
- Forecast agrees with the real step path tick-for-tick at exact minutes: forecast.test.ts.
- Queue invariants green: 10k-edit property test.
- `npm run verify` + `npm run harness`: both exit 0 (**163 tests / 19 suites**, incl. the round-1 and round-2 critic-fix suites).
