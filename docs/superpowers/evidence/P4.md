# P4 Evidence — queue UI and first session (2026-07-30)

**Plan:** `plans/2026-07-30-p4-queue-ui-first-session.md`
**Protocol:** `plans/2026-07-30-p4.5-external-playtest-protocol.md` (frozen at T0)

## What landed

- One application-owned top-level tick: deterministic game commands → sim step →
  game/session fold → composed snapshot.
- The complete P4 queue surface: insertion, object click, reorder/pin, details and
  forecast, Stop, remove, receipt-backed Undo, keyboard paths, drag paths, urgency,
  and the night-sleep skip.
- The first-session slice: Practice, the Day-1 package and two-decoration choice,
  Goals 1–2, durable in-run decorations, and the first-morning recap.
- Authored first-session copy is schema-checked and covered by the Humanizer v2.2.0
  review manifest. The reviewed file hash is
  `71e50effdd8a4b0a6378ff1f12f5902dbbdfda5f17e2bacc19d9c21c56f97c50`.

## ENGINE_VERSION 5 → 6

P4 changes player-driven simulation behavior, so the version bump is intentional:
typed command outcomes, wrinkle provenance/ordering, move-to-pin, stop/remove/Undo
identity and suppression, system-only activities, plus the application-owned game
action/session path.

Two goldens pin the boundary:

| Replay | Reviewed result |
|---|---|
| Unattended seven-day golden | The snapshot diff is **only** `engineVersion: 5 → 6`. Every per-day bar, completion, travel, urgent, missed-anchor, and wake-order value is unchanged. |
| Scripted-player golden | New top-level replay, seed 1234. Full sim + game state SHA-256: `20c91a9fa7c887b75c2b02fa927cc340c1ae78fc1930362cb66d719f3b7ea842`. Forecast and actual start both land at minute 434. |

The scripted replay covers, in one deterministic run:

- duplicate Practice cards with distinct durable IDs;
- object-click insert and promotion;
- moving an AUTO card and observing it become PINNED;
- Stop, remove, same-ID Undo, and AUTO suppression;
- serializable why-line and forecast-observed actions;
- the Day-1 package, Goal 1, Goal 2, Journal, both decoration sources, and the
  first-morning recap.

The P4-owned adjacency pairs each execute a real nonzero effect in the engine:
`warmed-up`, `cramp`, `minty-fresh`, and `fresh-mind`.

## Verification before browser acceptance

- Content: 10 registries/files, including both authored string batches; 2 current
  writing reviews.
- Atlas: 512×162, 33 sprites, 29 colours, 6 tiles, 15 objects, 9 character sprites.
  Two consecutive generations were byte-identical:
  - PNG: `df1788ec6d3de11eebf1025c62d59959b35e36932c869c3a0e620b98ad4e3916`
  - index: `bffc1f1a168d97e58ee0b625c2a55d7cb2afde62b752b53ba70703b1890edc3d`
- TypeScript: clean.
- Jest: **51 suites / 419 tests / 2 snapshots green**.
- Web export: green; the exported bundle contains the regenerated atlas.

The repository’s combined `verify` command cannot pass its `git diff` art-cleanliness
subcheck while the newly generated atlas is intentionally uncommitted. No files were
staged or committed to hide that state. Generator reproducibility is proven by the
exact hashes above; the remaining test/export gates are recorded after T12.

## Exported-browser acceptance

T12 ran against the exported desktop-web build at `http://127.0.0.1:4173/`.
The source base was `fc45b39` plus the intentionally uncommitted P4 tree:

- `ENGINE_VERSION`: 6
- fixed live/replay seed: `1234`
- app bundle: `App-fe0ffa0b66a7523941714c8998b3d662.js`
- app bundle SHA-256:
  `405046acc6af273cea2ba156dcf99a1380b01914488e439940f582d2a5bbee5a`
- complete P4-baseline `dist/` hash (SHA-256 of its sorted eight-file hash manifest):
  `b02b7d4be91e001e2ac7c4324d4b2c115670a323a8bfb3af96800f7a88fb8cca`

Joe explicitly moved the counted P4.5 sessions until after P5 on 2026-07-30.
This hash proves P4 completion; the playtest fields must be frozen again from the
post-P5 export.
- exported atlas SHA-256:
  `df1788ec6d3de11eebf1025c62d59959b35e36932c869c3a0e620b98ad4e3916`
- tested desktop viewports: 1366×768 and 1024×768
- both default and touch-matrix sessions: empty browser console and page-error logs
- required headers active: `crossOriginIsolated === true`

### Input matrix

| Surface | Mouse | Keyboard | Touch-capable web emulation |
|---|---|---|---|
| Palette insertion | Room palette opened and inserted Practice | `A`, native traversal and activation inserted an item | Palette opened and inserted Snack |
| Card menus and details | Current, upcoming and block menus; Details and Do next | `Q`, arrows and `Enter` opened the focused menu and actions | Expanded-step menu, Do next and Details |
| Stop | Current-card Stop | `X` | Current-card touch Stop |
| World objects | Guitar plus Sink choice → Quick wash | Focused `Enter` activated Guitar and Sink → Quick wash | Guitar plus Sink choice → Quick wash |
| Expanded block editing | Expand, step menu and Do next | `Q` → arrow → `Enter` expanded the real block | Expand, step menu and Do next |
| Remove and Undo | Menu remove and drag-off remove restored from the opaque receipt | `Delete` then `U`, inside the five-second window | Drag-off removal published Undo; touch Undo restored the same card id |
| Panels and shortcuts | Palette, choices, Goals and recap panels | Space, 1/2/3, `A`, `Q`, arrows, Shift+arrow, Enter, Delete, `U`, `X`, `G`, and Escape | All pointer-owned panels opened and closed by touch; keyboard-only shortcuts are not applicable |
| Drag semantics | 4 px stayed a tap; reorder; return-to-origin cancellation; drag-off removal; 832/1188 horizontal overflow; reorder after scroll | Shift+arrow supplies full non-pointer reorder parity | 4 px stayed a tap; reorder; return-to-origin cancellation; drag-off removal + Undo; touch-scrolled 832/1482 overflow to `scrollLeft=470` |

The touch row is emulation, not a physical device. It was not inferred from an
iPad user-agent: the browser protocol explicitly enabled touch and the page reported
`maxTouchPoints=5`, `(pointer: coarse)=true`, and `(hover: none)=true`. Captured
events included `touchstart`/`touchend` and pointer events whose
`pointerType === "touch"`.

Local captures:

- `/tmp/life-sim-t12-full-queue.png`
- `/tmp/life-sim-t12-g-shortcut.png`
- `/tmp/life-sim-t12-real-touch.png`
- `/tmp/life-sim-t12-touch-matrix.png`

### Live first-session smoke

One continuous exported-browser run:

1. inserted and completed Practice, moving the permanent HUD counter from 0 to 33;
2. opened a real anchor why-line and observed a changed forecast;
3. received the 10:00 package and chose the sunny vase;
4. completed Goals 1–2 and displayed “First steps complete”;
5. kept both the Goal-1 plant and chosen vase visible in the world;
6. reached night Sleep, crossed the real ten-second quiet gate, and advanced to Day 2;
7. displayed and expanded the first recap: 19 activities, 3 meals, 69 Practice,
   with the missed Workout and Dinner routines named.

The permanent-reward capture is
`/tmp/life-sim-t12-decoration-practice33.png`; the recap captures are
`/tmp/life-sim-t12-first-recap.png` and
`/tmp/life-sim-t12-first-recap-expanded.png`.

### Minimum slice and reset

All nine required pieces are present:

1. autonomous routine;
2. editable queue;
3. visible starts, cap-waste, conflicts and why-lines;
4. Practice with its time cost and block bonus;
5. Day-1 package wrinkle;
6. Goals 1–2;
7. visible decoration reward;
8. first-night recap;
9. deterministic reset plus run-long decoration visibility.

The final reset regression mutates every loop-owned P4 layer before reset: sim and
session state, pending commands and game actions, forecast revision, accumulator and
stats, speed and system pause, receipt/toast, and idle state. The mounted UI test
proves presentation-owned Goals state also closes on a fresh session.

The free-time-fill check measured **722 idle minutes unattended versus 286 with ten
Practice cards** on seed 1234. The test asserts a material reduction (at least 30%)
without freezing future P4.5 tuning to those exact values.

### Final T12 verification

- Jest: **52 suites / 421 tests / 2 snapshots green**
- TypeScript: clean
- Content: 10 registries/files and 2 current writing reviews
- Web export: green
- Exported browser: full matrix above; no console or page errors

The combined `npm run verify` wrapper still cannot pass its intentional
`git diff --exit-code -- assets/generated` cleanliness check while P4's generated
atlas remains uncommitted. No files were staged or committed to disguise that state;
the source and exported atlas hashes match exactly, and all underlying gates passed.

## Post-T12 P0–P4 audit resolution

Joe supplied a separate full-phase audit after T12 and required only high-impact,
high-confidence changes before P4.5.

| Audit finding | Verification and disposition |
|---|---|
| Goal 2 auto-counted every forecast revision | **Confirmed and fixed.** A revision still publishes the polite “Forecast updated” announcement, but it now only arms an observation. The `forecastChangeObserved` action is sent when the player deliberately opens Details while a changed forecast is waiting to be viewed. |
| T12 still open | **Already closed** by the complete matrix and live first-session smoke above. |
| Idle-fill gate unmeasured | **Already closed** with the recorded 722 → 286 comparison and regression assertion above. |
| P4 worktree uncommitted | **Confirmed; no Git write performed.** Committing was not explicitly authorized. The exact candidate bundle and atlas hashes are recorded, but commit/push safety remains a user-controlled Git step. |
| `QueueStrip.tsx` is a P5 maintenance tax | **Confirmed at 1,886 lines, deliberately deferred.** It is not a P4 completion blocker. Evaluate a narrow extraction before new P5 queue work; do not turn it into a speculative rewrite. |

The lower-impact nits were triaged without speculative cleanup: T10d’s stale checkbox
is corrected; dead `ProofScreen`/`AtlasProof` files and the default health-band proxy
remain untouched because neither affects the completed P4 baseline.

### Goal 2 regression proof

The mounted queue test now proves all three required states:

1. a forecast revision updates the live announcement;
2. that revision alone does **not** call `onForecastChangeObserved`;
3. opening Details after the revision calls it exactly once.

The fresh export repeated the real path. After adding Practice, the Goals surface read
“plan edited, forecast change not seen.” After opening Practice Details and advancing
one tick, it read “Done” and the bedroom plant appeared. Capture:
`/tmp/life-sim-audit-goal2-deliberate.png`.

This changes UI sampling, not deterministic sim math or the serialized action/replay
format, so it does not require another `ENGINE_VERSION` bump.

The real-touch matrix was rerun in full on the updated candidate; all checks passed
again, including Details, Stop, block editing, threshold/cancel/reorder, overflow,
drag-off removal and same-id Undo. The default and touch sessions again had empty
console and page-error logs.
