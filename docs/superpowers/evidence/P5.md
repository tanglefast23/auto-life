# P5 evidence — staged v1 systems, settings, and saves

> Living evidence record. P5 implementation began only after Joe approved the
> consolidated plan. Passing P5 will produce the build for P4.5; it does not
> pass the human playtest.

## T0 — implementation-start baseline

- Branch: `p4-queue-ui`
- Commit beneath the working tree: `ad347a41e4e6d4d0befa114e8d8e18f648180c83`
- Current pre-P5 engine: 7
- Node: 22.23.1
- npm: 10.9.8
- Lockfile: 3
- Expo: 57.0.8
- React Native: 0.86.0
- Zod: 4.4.3
- Zustand: 5.0.14
- TypeScript: 6.0.3
- Jest: 29.7.0
- New P5 dependencies: none

### Exact start-tree verification

`npm run verify` passed on 2026-07-30:

- content/writing validation: 10 files, 2 current writing reviews;
- generated atlas: 512×162, 33 sprites, 29 colours, byte-clean;
- strict TypeScript;
- 56/56 Jest suites, 431/431 tests, 2/2 snapshots;
- exported web bundle:
  `_expo/static/js/web/App-4d9604ff9924dc3b81c170785c1bc08e.js`.

The first sandboxed attempt was blocked before project code ran because `tsx`
could not open its temporary IPC socket (`EPERM`). The approved unrestricted
rerun above is the counted result.

### Compatibility fixtures

P4 did not ship career disk persistence. These are synthetic compatibility
fixtures, not claims about real player saves:

- `src/application/__fixtures__/engine-v6-career.json` was generated and
  validated from archived commit `ad347a4`; it captures a mid-travel activity,
  removal receipt, P4 goals, resolved Day-1 package, decoration, queue
  provenance, and all five PRNG streams.
- `src/application/__fixtures__/engine-v7-career.json` was generated from the
  exact post-audit working tree; it captures travel toward a lunch anchor,
  PINNED reactive provenance, and registry-resolved IDs.

The target for the first P5 schema/replay change is `ENGINE_VERSION = 8`.

## T1 — content and writing boundary

- The single runtime `ContentRegistry` now owns goals, intentions, identity
  presets/preferences, wrinkles, storylets, and a stable authored-string
  catalog alongside every P0–P4 content file.
- Cross-file validation preserves the v7 activity/object, planner, and
  fixed-point checks and now rejects duplicate IDs, dangling reward/string
  references, bad windows, unsupported mechanics, impossible wrinkle success
  conditions, and variants without an explicit player action.
- All eight canonical wrinkles are present as data: six reusable shapes plus
  availability-gate and wake-modifier.
- Writing validation discovers every authored JSON file in
  `content/strings/`, excluding only its manifest. Missing/extra/duplicate
  reviews, stale hashes, mismatched string IDs, and invalid rewrite records
  fail the gate.
- Humanizer 2.2.0 was applied to the complete five-file P5 batch with the
  `writing.md` checklist. The manifest contains seven current reviews total.

Focused verification on 2026-07-30:

- `src/sim/__tests__/p5-content-registry.test.ts`: 6/6 passed.
- `scripts/__tests__/validate-writing.test.ts`: 5/5 passed, including the
  checkpoint case where a new unreviewed file fails for the expected reason.
- `npm run typecheck`: passed.

The sandboxed standalone `validate:content` command was blocked before project
code ran by the same `tsx` temporary IPC restriction recorded in T0. Its
unrestricted result is folded into the next full verification gate.

## T2 — engine-v8 career envelope

- `ENGINE_VERSION` is now 8. The reviewed unattended golden changes only its
  engine metadata; its seven-day mechanics remain byte-for-byte the same.
- `CareerState.payload` now owns sim, game, identity, the five PRNG streams,
  autonomy, pending boundary commands/actions, and the deterministic rules
  revision. App preferences use a separate versioned envelope.
- v6 and v7 synthetic fixtures both migrate to v8. Their five PRNG stream
  records are exactly equal before and after relocation; no history is replayed.
- `SimRules` is derived once per boundary and the same value reaches live
  `step()` and `forecast()`. Its revision/key are present in the canonical
  scripted replay.
- The post-boundary game observation and calendar ledger record midnight and
  wake+2h independently from wake-based recap presentation.
- The initial canonical v8 payload digest is
  `e68b947ff8fc8c202f97359a70147b662bcd6a8c75282c1e245e4166968b69e1`.
  The scripted-player canonical payload digest after its first-day replay is
  `d6c7ecd8e2dc31bca3df7aa6d2e033cc1382782a89910880a1bbcb9edcbfcb79`.

Full checkpoint verification on 2026-07-30:

- content gate: 14 registry files and 7 current writing reviews;
- generated atlas: 512×162, 33 sprites, 29 colours, byte-clean;
- strict TypeScript;
- 61/61 Jest suites, 454/454 tests, 2/2 snapshots;
- exported web bundle:
  `_expo/static/js/web/App-b841fd955e30ca822663631af61c70d1.js`.

## T3–T4 / T10 / T12 — save-safe application spine

- Three rotating generations now load newest-valid, preserve unreadable blobs
  for recovery, and reject stale second writers. A durable reset fence prevents
  an older career from returning after any partial New Game failure.
- Preferences load before career data, and the loop does not exist until a
  complete career has been selected or created. Restored sim, game, PRNG, and
  pending boundary work are the first published frame with zero away-time
  ticks.
- Title, identity-lite, deterministic skip, four appearances, and exactly two
  visible active preferences are mounted. Early/owl traces shift wake, bed,
  morning check, reactive windows, and wrinkle windows by the same hour.
- The pause/settings surface implements all eight SPEC groups. App-global
  preferences, deterministic career state, and session-only speed/panels stay
  separate. New Game and Reset use the same double-confirmed reset fence.
- The silent audio bus owns persistent master/music/SFX/mute values; `M` and
  the visible control update and announce mute state without adding an audio
  package or asset.
- Save requests come from accepted command outcomes, game actions, and domain
  events. Queue edits use a 500 ms trailing debounce, ordinary foreground
  writes coalesce to at least five seconds, periodic writes run at 60 seconds,
  and visibility/pagehide/Return-to-Title use the typed lifecycle barrier.
- A mid-activity checkpoint with pending command/action work resumes to the
  exact same canonical payload as an uninterrupted control. Inaccessible Undo
  receipts expire on load; interpolation, current speed, open panels, and toast
  visibility never enter the career.

The settings prose received Humanizer 2.2.0's full pattern, read-aloud, and
anti-AI pass. `content/strings/settings.json` is registered with its exact
hash, IDs, and `writing.md` checklist; the manifest now contains nine current
reviews.

### Gate A verification

`npm run verify` passed on 2026-07-30:

- content gate: 14 registry files and 9 current writing reviews;
- deterministic atlas: 512×162, 33 sprites, 29 colours, byte-clean;
- strict TypeScript;
- 72/72 Jest suites, 504/504 tests, 2/2 snapshots;
- exported web bundle:
  `_expo/static/js/web/App-8363e10de3a0172514125846948e2839.js`.

The first Gate A rerun exposed one migration-only edge: the synthetic neutral
chronotype has no player-facing preference label. It now stays invisible
instead of crashing; real new careers still roll early or owl. The focused
boot/tag check and the complete rerun above both passed. The P5 envelope is
frozen at engine v8 for Milestone B.

### Gate A exported-browser proof

The counted Gate A browser smoke used the exported bundle above through the
local static server in Headless Chrome 150 at 1280×577. The final P4.5
environment is not frozen from this smoke; T14 owns the two required desktop
viewports and final build hash.

Observed against the real exported app:

- fresh title → identity → game created a rotating-generation career;
- a debounced queue edit saved a complete mid-travel checkpoint and a hard
  reload restored the pending Practice card before ordinary ticking resumed;
- a hard reload from a saved mid-travel Practice card restored its pending
  destination, and a separate saved Shower at 0/18 ticks restored visibly at
  6% with the “Nothing moved while you were away” notice;
- corrupting only the newest generation loaded an older valid generation and
  left the unreadable raw slot intact;
- after 15 seconds in another tab, the game clock moved from 13:22 to 13:23
  when the app returned, proving there was no hidden-time catch-up;
- a second live app tab wrote a newer generation; the original tab immediately
  parked at “This life is open elsewhere” and allowed no further changes.

The browser reported no page errors and no console messages during the counted
smoke. Exact mid-Shower and mid-travel payload equality, delayed/failing-driver
races, and reset-fence failure points are also covered by the Gate A automated
suite; T14 reruns the complete post-P5 browser matrix on the frozen build.

## T5 — daily intentions and visible preferences

- All five intention rows now have one saved, deliberate selection per day.
  Balanced leaves the planner unchanged; Take it easy suppresses the workout
  anchor and favors Quick wash; Get moving suggests one mid-morning Stretch and
  removes the second workout's cross-cost; Eat properly chooses Meal first and
  lowers the well-fed threshold to 60; Practice focus offers a player-accepted
  two-card PINNED block and never schedules Practice on its own.
- Goal 5 records only a favored completion strictly after that day's deliberate
  choice. Earlier completions, non-matching activities, a disabled prompt, and
  non-deliberate defaults cannot satisfy it. The wake boundary archives one
  selection and opens the next day.
- Food and workout preferences now affect only planner choice. A matching
  completion produces a small happy bubble; only repeated overrides produce a
  grumble. The reaction is presentation-only and cannot mutate or reject a
  player command.
- The same immutable `SimRules` reaches live stepping and forecast clones.
  Inactive/default preference policy preserves the pre-T5 rules key and the P4
  scripted golden.
- Humanizer 2.2.0 and the full `writing.md` checklist were rerun over the
  complete intentions file after adding the picker and bubbles. The current
  reviewed hash is
  `be10145e5e47b0b41bd13c7f32d1b0442be28c91724a4b01df67c00944a8f761`.

T5 checkpoint on 2026-07-30:

- one focused mechanical test per intention, plus prompt/Goal-5 and preference
  presentation tests;
- two deterministic 1,440-tick traces closed at the exact next wake;
- content and writing validation, strict TypeScript, and the complete Jest tree
  passed with the existing scripted-player snapshot unchanged.

## T0A — behavior-preserving UI extraction

- `QueueStrip.tsx` now keeps queue orchestration and focus ownership while its
  existing cards, menus, Details, and palette render through
  `QueueStripCards.tsx` and `QueueStripPanels.tsx`.
- `FirstSessionUI.tsx` keeps panel state and daily-intention orchestration while
  its existing goals, package choice, and morning recap render through
  `FirstSessionPanels.tsx`.
- Public props, commands, strings, test IDs, focus callbacks, and the original
  style objects are unchanged. No test or snapshot edit was required for this
  extraction.
- Strict TypeScript and the four existing mounted/presenter/drag suites passed:
  4/4 suites and 26/26 tests.
- The exported bundle
  `_expo/static/js/web/App-741095eeaadb525c0507ce0c5be45718.js` was opened in
  Headless Chrome 150 at 1280×577. Title → identity → day one, Goals, and the
  queue palette all rendered and remained interactive, with no page or console
  errors.

## T6 — seeded wrinkle deck and explicit start decisions

- Day 2+ now deals from the saved `wrinkles` stream with quiet days, one deal
  per calendar day, and a six-calendar-day no-repeat window. The scripted
  Day-1 package never re-enters the reusable deck.
- The eight registered mechanics each have an exact engine test:
  package/visitor, repair/block, favorite-show/timed-window,
  headache/slowdown, slept-great/free-time,
  burned-breakfast/forced-substitution, empty-fridge/availability-gate, and
  rough-night/wake-modifier. Their anchor precedence is validated at the
  content boundary and exercised through the shared live/forecast rules.
- Card startability at the wrinkle seam is now `start | wait | reroute`.
  Waiting preserves card identity and position. Only an urgent automatic
  Hygiene card can reroute to reachable Quick wash; other Shower cards remain
  visibly blocked.
- Slept-great changes only that day's sleep end and wake energy. It does not
  rewrite the permanent chronotype, anchor targets, or reactive windows.
- Dealt IDs, variant IDs, selected parameters, deck state, and named PRNG
  calls survive exact save/resume. The resumed boundary does not draw or deal
  again. A deal and its player action emit semantic save reasons.
- The player-facing daily wrinkle surface now announces the reviewed intro,
  exposes the registered action, waits behind the wake recap instead of
  overlapping it, and remains reviewable from a chip. The GameScreen callback
  reaches the typed `wrinkleAction` reducer path.
- Humanizer 2.2.0 was rerun over the complete wrinkle and queue batches. The
  current wrinkle strings hash is
  `2a42f2a37a9e6f044aa6f7476b842996e65440c3dc24f1f2d0bdc4ab4cf7054d`;
  all nine authored-string reviews are current.

T6 checkpoint on 2026-07-30:

- `npm run check`: content/writing gate, strict TypeScript, 77/77 Jest suites,
  539/539 tests, and 2/2 snapshots passed.
- The reviewed scripted-player snapshot now records the resolved Day-1 package
  outcome and the deterministic pending Day-2 deal; its replay remains
  byte-deterministic.
- Exported bundle:
  `_expo/static/js/web/App-a738a204cadc18b52e9f8613415b6a67.js`.
- Headless Chrome 150 at 1280×720 loaded a deterministic repair-day career.
  It showed the daily Repair visit panel and accepted “Plan around the
  repair,” then showed `Blocked` and `Quick wash` chips together. Their Details
  lines were “Shower is blocked until 09:30.” and “Shower blocked. Quick wash
  runs instead.” The page and console error logs were empty.

## T7 — Practice levels, Goals 3–6, rewards, and Routine memory

- Practice L0–L3 is derived from the existing cumulative integer ×100 total at
  the authored 100/300/700-point thresholds. Scoring, curves, and multipliers
  are unchanged.
- One typed evaluator now owns all seven authored goal conditions. Goals 1–2
  grant their existing rewards automatically; Goal 3 waits for one of two
  distinct decoration choices; Goal 4 records the air-guitar unlock; Goal 5
  places the Practice-poster placeholder; Goal 6 unlocks Routine memory.
  Reward status makes every grant idempotent across a completion-boundary
  reload.
- Goal 3 samples the completed calendar day exactly once at midnight and
  requires a resolved wrinkle with zero urgent events. A quiet day does not
  count. Goal 6 samples the prior day exactly once at wake+2h and resets unless
  that day had at least two completed Practice sessions, zero urgent events,
  and all four bars at 65 or higher. Both goals remain visible but cannot
  progress below Full routine.
- The saved three-day Routine-memory history records first reactive completion
  order with a fixed absent-day sentinel. Its total order is exact integer
  score bucket descending, scalar routine rank ascending, enqueue tick
  ascending, then stable card ID code-unit ascending. It sorts only within the
  existing automatic run, so PINNED barriers and anchor blocks remain intact.
- Property tests cover antisymmetry, transitivity, and every permutation of a
  tied run. Focused tests also cover exact score-bucket half boundaries, the
  three-day bound, streak break/reset, restricted autonomy, duplicate samples,
  all reward paths, and a pending Goal-3 choice resumed from a complete
  boundary.
- The permanent HUD now shows `Practice L0–L3 · points`. The data-backed Goals
  & Journal panel renders all seven authored goals, explicit progress,
  locked/rewarded states, Practice level, the Full-routine requirement, and the
  Goal-3 choice. Goal-3 and Goal-5 placements use distinct IDs with existing
  atlas sprites as deliberate P6 placeholders.
- Humanizer 2.2.0 and the `writing.md` checklist were rerun over the complete
  goals batch. Its current reviewed hash is
  `2bf16824fcb132667f25f8a8e51552dd528a22a2324cd90070b8222f75467ff7`.

T7 checkpoint on 2026-07-30:

- `npm run check`: content/writing gate, strict TypeScript, 80/80 Jest suites,
  553/553 tests, and 2/2 reviewed snapshots passed.
- Exported bundle:
  `_expo/static/js/web/App-3543dc5274f71a9228a86c85b3a72cfe.js`.
- Headless Chrome 150 at 1366×768 showed the level in the HUD and all seven
  goals in the scrollable Goals & Journal panel. The panel remained open over
  live ticks. This browser pass caught and fixed an earlier reset check that
  closed the panel after ordinary progress. The exact final export had no page
  errors or console messages.

## T8–T9 — Journal, complete recap, and progress UI

- `SessionState` remains the only owner of recap and Journal state. Stable
  Journal IDs record day, minute, source kind/source ID, and reviewed string
  ID. Reloading the same boundary cannot append the same entry twice.
- One daily storylet is selected at midnight, with a wake fallback when needed.
  Only the saved `storylets` stream is consumed. The registered pools cover
  wrinkle outcomes, milestones, and idle moments.
- The wake recap now reports ending bars against the prior day, meal count,
  Practice points and counted Practice sessions, wrinkle outcome, goal
  progress/rewards, missed routines, and one Journal line. The saved ending
  bars survive the wake rollover instead of being replaced by the new day.
- Watched sleep and sleep-skip produce the same deterministic career payload,
  game session, PRNG state, recap, and Journal. Midnight, wake, and wake+2h
  ordering tests prevent a completion from landing on two days.
- Goals & Journal now exposes all seven goals, filtering, progress and
  locked/rewarded states, reverse-chronological Journal entries, preference
  tags, today's intention, today's wrinkle, reward choices, and the persistent
  letter summary. The recap remains non-modal, expandable, dismissible, and
  announced through a live region.
- `G`, `Escape`, and the pause-menu shortcut share the same panel owner.
  Exported-browser observation confirmed that Escape closes Goals and returns
  focus to its opener.
- A final exported-browser accessibility check found a concrete issue:
  identity radios and settings radio/switch controls looked selected but did
  not expose `aria-checked`. Explicit typed checked state was added to both
  surfaces and covered by mounted tests. The final DOM reported the expected
  true/false values.

Final reviewed writing hashes for these surfaces:

- `content/strings/first-session.json`:
  `b4ba22882b535069c3cbf253f946026196511a3d295266f0dec1d440ccf6e132`
- `content/strings/goals.json`:
  `e6f95c120e2d8dfe288bcd5f272f1c4813775332c3ac1e5e392db6e46905fa4b`

## T11 — Day-8 letter and terminal handoff

- Goal 6 may open the letter on Day 8. Day 10 is the hard fallback, including
  a career loaded after that boundary. A decline schedules the next Sunday
  strictly after the response day; reload and repeated due checks are
  idempotent.
- A due letter stops ordinary ticks, hydration, and sleep-skip before a later
  boundary. The game pauses for the response and restores the prior running
  speed afterward.
- Acceptance snapshots Practice level and the Prepared Performer bonus
  (`0/20/50/100%`) without spending mastery points, records the next weekday
  start, completes/rewards Goal 7, and persists the promise in Goals.
- The accepted panel states that v1 ends here and the first shift belongs to
  the next chapter. No Funds, job, venue, shift, career-GP, or Week Plan system
  was introduced.
- The exact final export was loaded from a disposable Day-8/Goal-6 fixture at
  1024×768. The letter appeared at Day 8, Mon, 10:00 before time advanced.
  Acceptance saved `status=accepted`, `acceptedAtDay=8`,
  `promisedStartDay=9`, Practice L0, bonus 0%, and rewarded Goal 7. The
  persistent Goals summary then named Tue, Day 9 and the next chapter.

The letter batch passed Humanizer 2.2.0 and the full `writing.md` checklist.
Its current reviewed hash is
`a3ec4d755a8444e8402462f45b4f5255e70618895e6cc19b904002801e3673e4b`.

## T13 — week replay, deterministic exposure, and goldens

- The existing scripted-player golden now runs from a real engine-v8 career
  payload through the start of Day 12. It deliberately chooses a daily
  intention, schedules two Practice sessions per day, takes every dealt
  wrinkle action, resolves Goal 3's choice, and declines the Day-8 letter.
- Its digest proves all eight canonical wrinkle mechanics, Goals 3–6 rewarded,
  the Practice poster, decoration reward, Routine memory, at least seven
  followed intention days, every reactive band, at least ten Journal entries,
  and the declined/re-offer letter state.
- The fifth player-dependent adjacency, `it-sticks`, now has a nonzero-effect
  assertion alongside P4's four pairs.
- The final canonical scripted-career SHA-256 is
  `141101f2b3b8b6a81c990b886c44e0fe6e03f7e2b67eb0a35f50034af6c3324e`
  at seed 1234 and `ENGINE_VERSION=8`. A second complete run produced the same
  digest.
- The unattended seven-day golden and fairness/balance harness remain
  separate. The unattended mechanics stayed unchanged apart from the already
  reviewed engine-v8 metadata. Both v6→v8 and v7→v8 fixtures remain green.

## T14 — final exported-browser acceptance and P4.5 freeze

### Counted local release gate

`npm run verify` passed on 2026-07-30:

- content/writing gate: 14 registry files and 10 current writing reviews;
- deterministic atlas: 512×162, 33 sprites, 29 colours, byte-clean;
- strict TypeScript;
- 83/83 Jest suites, 587/587 tests, and 2/2 snapshots;
- final web export:
  `_expo/static/js/web/App-65586a85f2fd66e4542bd3fca5b0fe6b.js`.

The final app-bundle SHA-256 is
`3a27918cf198973555da982066ce9fb0616fedb0aef7fd688a369ff10a9789d4`.
The complete sorted eight-file `dist/` manifest SHA-256 is
`427380ccba7a4018154ec82d134e69272c18e14119567332943ca3be17eaf85b`.
The manifest digest is the SHA-256 of newline-terminated rows in the form
`<file sha256><two spaces><relative path>`, sorted by relative path.

This is a local export, not a deployment and not remote CI.

### Frozen deterministic test-session reset

The browser pass exposed one exit gap before the freeze: production New Game
used browser crypto, so three playtests could not truthfully share one seed.
The smallest fix adds an explicit `?playtestSeed=<uint32>` mode. Normal player
starts remain crypto-seeded. In playtest mode, both first start and the
ordinary double-confirmed New Game/reset fence reuse the recorded seed.

The exact final export created and then reset the test career twice. Both
starts produced:

- root seed `1234`;
- engine `8`;
- Rowan, they/them, plum-night appearance;
- Night owl plus Window gazer;
- Full routine autonomy;
- Day 1, Monday, at the 07:30 owl wake target;
- no prior goals, rewards, Journal entries, or letter response.

The reset used the real Pause → New Game → Yes, start over → Randomize and
start path and advanced the durable reset fence from 0 to 1.

### Browser observations

The exact installed browser binary was Google Chrome `150.0.7871.187`
(`Browser.getVersion`), on macOS, at DPR 1. The final build was observed at
1366×768 and 1024×768. The P4.5 viewport is frozen at **1366×768**.

Observed across the final pass and the save-safe Gate A pass:

- title, identity, deterministic randomize, fresh career, and repeated New
  Game/reset;
- queue palette, object actions, current/upcoming menus, Stop, Details,
  Practice-focus two-card block, Goals/Journal, intention picker, pause menu,
  all settings groups, and keyboard focus return;
- exact mid-activity/travel hard reload, no hidden-time catch-up,
  corrupt-newest fallback, and stale second-tab parking;
- the representative repair/block/Quick-wash wrinkle surface, plus all eight
  mechanics through deterministic engine and mounted-UI tests;
- Day-8 letter pause, acceptance, saved Goal-7 outcome, and terminal summary;
- no console messages or page errors.

### Post-audit and player-feedback stabilization

Two independent P5 audits were checked against the live tree rather than
accepted wholesale. Nine reproduced, high-impact findings were fixed:
corrupt-reset recovery, urgent HUD speech, a reversible New Game identity
overlay, Day-2 forecast adjacency, duplicate AUTO Undo, whole-block movement,
Goals-panel ownership, paused queue edits, and release provenance. Lower-impact
or speculative suggestions were left alone.

Joe's three playtest findings were also reproduced and fixed:

- movement is snapped to the physical pixel grid while preserving sub-tile
  interpolation, removing the retained-looking silhouette;
- settings and mute controls now participate in the clock block's layout
  instead of overlapping the speed row;
- the active card always renders its activity name.

The queue is now a fixed 224 px right rail. It reserves world width, renders one
task per row in one column, scrolls vertically, and moves whole collapsed blocks
with vertical drag or Up/Down keys. At 1366×768 the rail measured
`x=1142..1366`, and at 1024×768 it measured `x=800..1024`; queued rows began
below the clock block at both sizes. A paused edit appeared immediately without
advancing the clock. Three separate 4× movement frames each showed one crisp
character at a different world position. The final browser run reported no
console messages or page errors.

These corrections landed before the first engine-v8 build was committed or
frozen, so they remain part of the consolidated P5 `ENGINE_VERSION = 8`
checkpoint rather than creating a second unshipped version. Both reviewed
goldens passed unchanged, including canonical scripted-career SHA-256
`141101f2b3b8b6a81c990b886c44e0fe6e03f7e2b67eb0a35f50034af6c3324e`.

Mouse and keyboard were directly observed in the export. Touch threshold,
reorder, cancellation, overflow, drag-off removal, and Undo remain covered by
the mounted responder/drag matrix; they were not repeated with physical touch
hardware in this final local pass.

Evidence images:

The three images below record the pre-stabilization P5 flow checks. The final
right-rail and movement checks were rerun live after the stabilization changes;
their measured results are recorded above.

- `p5-goals-1366x768.png`:
  `51384fde834e5792b73a905887c2b5dbcae836f79829ebff53d6fc49aa7e0c05`
- `p5-goals-1024x768.png`:
  `75e6f439cb2ead2baeee122772ef54e570476f984fbb332da5503984ad0b59c5`
- `p5-letter-1024x768.png`:
  `7bf2cb641c30756cb9cc870577d754830f032e0188874284a224a221a9faf0a7`

### Honest limits and exit verdict

- **NOT VERIFIED:** P4.5's diagnostic run and three fresh external human
  sessions. Joe explicitly deferred them until this P5 build was ready.
- **NOT VERIFIED / not P5:** physical iPhone acceptance, final sound assets and
  mix, final art/animation/juice, and P6 performance sign-off.
- **NOT VERIFIED:** remote CI and deployment. All evidence above is local.

**P5 passes its local implementation and freeze gates. P4.5 does not pass
automatically. P6 remains closed until Joe and at least three fresh friends
complete the unchanged protocol.**
